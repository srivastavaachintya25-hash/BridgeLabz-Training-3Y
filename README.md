# BridgeLabz-Training-3Ythis is readme file
this is my new readme line

git init
git add
git commit -m "[name]op: commit mssg"
git status

git push -u origin <branch name>

git branch -> to see all branch list
git checkout -branch <branch name> -> switched to other branch
git merge <branch name>

git fetch
git diff
git pull = fetch + merge

merge conflict